/* Data which has to be set to Layout Manager. Basically these sizes are basic for the game
 * It means that the sizes never will be less than these ones */
export default {
  "width": 960,
  "height": 960
};